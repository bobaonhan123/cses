// https://cses.fi/problemset/task/3359

/*
Như cây nhị phân, nếu tồn tại nhánh nào có thứ tự từ điển thấp thì tự động tỉa nhánh

*/

#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pb push_back
#define fast ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)

int main() {
    fast;
    int n;
    cin>>n;
    vector<string>s;
    for (int i = 0; i < n; i++) {
        cin>>s;
    }

    return 0;
}